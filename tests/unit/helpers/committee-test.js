import { committee } from 'legislator-lookup/helpers/committee';
import { module, test } from 'qunit';

module('Unit | Helper | committee');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = committee([42]);
  assert.ok(result);
});
