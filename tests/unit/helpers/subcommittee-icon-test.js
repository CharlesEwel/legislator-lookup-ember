import { subcommitteeIcon } from 'legislator-lookup/helpers/subcommittee-icon';
import { module, test } from 'qunit';

module('Unit | Helper | subcommittee icon');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = subcommitteeIcon([42]);
  assert.ok(result);
});
