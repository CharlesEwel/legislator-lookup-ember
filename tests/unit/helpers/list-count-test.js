import { listCount } from 'legislator-lookup/helpers/list-count';
import { module, test } from 'qunit';

module('Unit | Helper | list count');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = listCount([42]);
  assert.ok(result);
});
